---
wave: 4
depends_on:
  - 02-07-PLAN.md
files_modified:
  - zoho-ai-service/Dockerfile
  - zoho-ai-service/.dockerignore
  - zoho-ai-service/.gitignore
  - zoho-ai-service/tests/test_integration.py
  - zoho-ai-service/README.md
autonomous: false
---

# Plan 02-08: Integration, Containerization & Deployment Readiness

## Goal
Integration tests covering the full chain (FastAPI → Agent → ZohoDataService → mocked Zoho API), Dockerfile for containerized deployment, `.gitignore` finalization, and a deployment README.

## Tasks

---

<task id="integration-tests">
<read_first>
  - .planning/phases/02-ai-smart-budget-intelligence/02-AI-SPEC.md (Section 5 — Evaluation Strategy, Reference Dataset; Section 7 — Production Monitoring)
  - .planning/phases/02-ai-smart-budget-intelligence/02-RESEARCH.md (lines 468-516 — API Surface, FastAPI x LangChain)
</read_first>
<acceptance_criteria>
  1. `tests/test_integration.py` contains integration tests using mocked HTTP:
     - `test_full_chat_flow` — mock Zoho API HTTP responses to return project list + budget data, send `/chat` query "Which project is over budget?", verify response contains project name and the guardrail disclaimer is appended
     - `test_full_forecast_flow` — mock expense data via Zoho API, POST `/forecast` with valid project_id, verify response has `forecast_total`, `confidence_low`, `confidence_high`
     - `test_full_anomaly_flow` — mock expense data, GET `/anomalies`, verify `anomalies` list is populated
     - `test_full_weekly_flow` — POST `/weekly`, verify `emails_sent` is integer
     - `test_chat_guardrail_rejection` — POST `/chat` with out-of-domain query, verify 400 + rejection message
  2. All tests use `httpx.AsyncClient` against the FastAPI `TestClient`/`AsyncClient`
  3. All tests mock at the HTTP transport level (`httpx` mock or `resp`x) so no real network calls occur
  4. Tests run in < 30 seconds
</acceptance_criteria>
<action>
  1. Write `zoho-ai-service/tests/test_integration.py`:
     - Use `from fastapi.testclient import TestClient`
     - Fixture: mock `ZohoClient` shared across tests (patch `services.zoho_client.ZohoClient`)
     - `test_full_chat_flow`:
       - Mock `ZohoClient.get_records` to return project list + budget data
       - POST to `/chat` with `{"query": "Which project is over budget?", "user_id": "test"}`
       - Assert 200, assert response has `"response"` key
       - Assert disclaimer text appears in response
     - `test_full_forecast_flow`:
       - Mock expense records for 3 months
       - POST `/forecast` with `{"project_id": "test", "horizon_days": 30}`
       - Assert 200, assert `forecast_total` > 0, `confidence_low` < `confidence_high`
     - `test_full_anomaly_flow`:
       - Mock expense data with a spike
       - GET `/anomalies`
       - Assert 200, assert `anomalies` list returned
     - `test_full_weekly_flow`:
       - Mock projects + expenses + emailer
       - POST `/weekly`
       - Assert 200, assert `status` = "completed"
     - `test_chat_guardrail_rejection`:
       - POST `/chat` with `{"query": "What is the weather?"}`
       - Assert 400 (or response contains rejection message)
  2. Run `python -m pytest tests/test_integration.py -v` — all pass
</action>
</task>

---

<task id="containerization-and-readme">
<read_first>
  - .planning/phases/02-ai-smart-budget-intelligence/02-RESEARCH.md (lines 70-87 — Technology Decisions; lines 526-533 — Risks Q7, Q8)
  - .planning/phases/02-ai-smart-budget-intelligence/02-AI-SPEC.md (Section 7 — Production Monitoring lines 582-634)
</read_first>
<acceptance_criteria>
  1. `Dockerfile` builds a production image:
     - Base: `python:3.11-slim`
     - Installs `requirements.txt`
     - Copies `zoho-ai-service/` contents
     - Exposes port `8000`
     - CMD: `uvicorn api.main:app --host 0.0.0.0 --port 8000`
  2. `.dockerignore` excludes: `.env`, `*.pyc`, `__pycache__`, `.git`, `.pytest_cache`, `tests/`
  3. `.gitignore` includes: `.env`, `__pycache__/`, `*.pyc`, `.pytest_cache/`, `*.sqlite`, `.docker/`
  4. `README.md` documents:
     - Prerequisites (Python 3.11+, OpenAI API key, Zoho OAuth credentials, Langfuse instance)
     - Setup steps (clone, `pip install -r requirements.txt`, copy `.env.example` to `.env`, fill values)
     - Running locally: `uvicorn api.main:app --reload`
     - Running with Docker: `docker build -t zoho-ai-service . && docker run -p 8000:8000 --env-file .env zoho-ai-service`
     - API documentation (auto-generated FastAPI `/docs`)
     - Testing: `pytest tests/ -v`
     - Known limitations (from RESEARCH.md lines 520-533): report link names needed, milestone billing data availability, Langfuse self-hosting infra required
  5. Docker build completes without error: `docker build -t zoho-ai-service .` (or `docker` equivalent via `docker` commands if available, else mark as manual verification step)
</acceptance_criteria>
<action>
  1. Write `zoho-ai-service/Dockerfile`:
     - `FROM python:3.11-slim`
     - `WORKDIR /app`
     - `COPY requirements.txt .`
     - `RUN pip install --no-cache-dir -r requirements.txt`
     - `COPY . .`
     - `EXPOSE 8000`
     - `CMD ["uvicorn", "api.main:app", "--host", "0.0.0.0", "--port", "8000"]`
  2. Write `zoho-ai-service/.dockerignore`
  3. Update `zoho-ai-service/.gitignore` (or create if not exists at project root)
  4. Write `zoho-ai-service/README.md`:
     - Title: "ITOTCloud Budget AI Service"
     - Badge section (placeholder)
     - Prerequisites
     - Quick Start (local + Docker)
     - Environment Variables table (all 11 vars from .env.example)
     - API Reference table (6 endpoints)
     - Testing instructions
     - Architecture overview (1-2 paragraphs)
     - Known Limitations section
     - License (placeholder)
  5. Attempt `docker build -t zoho-ai-service .` — if Docker is available, verify exit code 0; if not, document as manual step
</action>
</task>

---

## Verification

1. `python -m pytest tests/test_integration.py tests/test_api.py tests/test_agent.py tests/test_zoho_client.py tests/test_cache.py tests/test_zoho_data_service.py tests/test_forecast.py tests/test_anomaly.py tests/test_emailer.py tests/test_scheduler.py -v` — ALL pass
2. `docker build -t zoho-ai-service .` completes with exit code 0 (or documented as manual)
3. `README.md` contains all required sections
4. `.gitignore` includes `.env` to prevent credential leaks
5. Integration test covers all 5 success criteria paths (chat, forecast, anomaly, weekly, guardrail rejection)

## Must Haves

- Full integration tests covering all 6 API endpoints with mocked Zoho API at the HTTP transport level
- Dockerfile with python:3.11-slim, pip install, uvicorn CMD
- `.gitignore` with `.env` entry
- README with complete setup, run, and testing instructions
- All 10 test files passing together (ensuring no cross-module import issues)
- Known limitations documented for deployment planning

## Threat Model

```xml
<threat_model>
  <threat>
    <id>T-22</id>
    <category>ASVS L1 — Insecure Docker Configuration</category>
    <description>Docker container runs as root by default. If an attacker gains access to the container, they have root privileges on the container filesystem, including access to .env credentials loaded at runtime.</description>
    <mitigation>Dockerfile adds a non-root user: `RUN useradd -m -u 1000 appuser && chown -R appuser:appuser /app` and `USER appuser` before CMD. The .env file is mounted at runtime (not baked into image).</mitigation>
    <severity>HIGH</severity>
    <status>BLOCKED</status>
    <blocking_note>Dockerfile MUST include USER appuser directive and MUST NOT copy .env into the image. The CMD should reference environment variables from --env-file or -e flags, not from a file in the image.</blocking_note>
  </threat>
  <threat>
    <id>T-23</id>
    <category>ASVS L1 — Unauthenticated API Access</category>
    <description>The FastAPI endpoints have no authentication in this phase. Any user who can reach the service port can query budget data via /chat, /forecast, /anomalies, /projects.</description>
    <mitigation>Documented in README as a known limitation. For production, add API key authentication middleware (FastAPI Depends) or deploy behind an API gateway with OAuth 2.0. The service is designed to run on an internal network only.</mitigation>
    <severity>HIGH</severity>
    <status>ACCEPTED</status>
  </threat>
  <threat>
    <id>T-24</id>
    <category>ASVS L1 — Dependency Vulnerability</category>
    <description>Third-party packages (langchain, openai, fastapi, etc.) may have known CVEs. A compromised dependency could exfiltrate Zoho OAuth tokens or OpenAI API keys.</description>
    <mitigation>All dependencies are pinned to specific versions in requirements.txt. A `pip audit` or `safety check` step should be added to CI/CD. For production, use a private PyPI mirror or scan with Snyk/Dependabot.</mitigation>
    <severity>MEDIUM</severity>
    <status>ACCEPTED</status>
  </threat>
</threat_model>
```

## Artifacts This Phase Produces

- `zoho-ai-service/tests/test_integration.py` — end-to-end integration tests (5 test functions)
- `zoho-ai-service/Dockerfile` — production container image
- `zoho-ai-service/.dockerignore` — build context exclusions
- `zoho-ai-service/.gitignore` — credential and build artifact exclusions
- `zoho-ai-service/README.md` — complete deployment and usage documentation
